[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "cfn-git-pipeline-migration"
version = "0.1.0"
description = "Migrate Control-M folder/job definitions: replace job commands from a CSV mapping and strip legacy job-name prefixes."
requires-python = ">=3.13"
dependencies = []

[project.scripts]
cfn-git-pipeline-migration = "cfn_git_pipeline_migration:main"

[tool.hatch.build.targets.wheel]
packages = ["src/cfn_git_pipeline_migration"]

[dependency-groups]
dev = [
    "ruff>=0.16.8",
]

[tool.ruff]
line-length = 100
src = ["src"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
